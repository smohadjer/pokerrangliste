{
  "name": "tournament-organizer",
  "version": "1.1.0",
  "description": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --testEnvironment=jsdom",
    "bundle:index": "esbuild public/ts/index.ts --bundle --format=esm --sourcemap --outfile=public/js/index.js",
    "bundle:admin": "esbuild public/ts/admin.ts --bundle --format=esm --sourcemap --outfile=public/js/admin.js",
    "start": "npm run bundle:index -- --watch=forever & npm run bundle:admin -- --watch=forever & npm run dev",
    "build": "npm run bundle:index && npm run bundle:admin",
    "dev": "nodemon server.js"
  },
  "author": "Saeid Mohadjer",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "esbuild": "0.18.20",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "dependencies": {
    "@vercel/edge": "^1.1.0",
    "dotenv": "^16.0.3",
    "jose": "^5.1.0",
    "mongodb": "^5.8.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
