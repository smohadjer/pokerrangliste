<header>
  <h1>{{event_name}}</h1>
  <div class="flex">
    <h2>Admin</h2>
    <div class="align-right">
      <p class="icon icon--inline icon--account">{{tenant_name}}</p>
      {{> logout }}
    </div>
  </div>
</header>
